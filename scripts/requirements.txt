# Phase 2 migration toolchain (run locally, no Docker).
# python -m venv .venv && source .venv/bin/activate  (Windows: .venv\Scripts\activate)
# pip install -r requirements.txt
PyMySQL==1.1.1          # read-only SELECTs against the imported legacy DB (Option A)
phpserialize==1.3       # decode ACF link/repeater values stored as serialized PHP
PyYAML==6.0.2           # emit clean YAML frontmatter
python-slugify==8.0.4   # deterministic slugs for routes + filenames
beautifulsoup4==4.12.3  # parse post_content HTML, harvest <iframe>/<a> blocks
markdownify==0.13.1     # HTML -> Markdown/MDX body conversion
python-dotenv==1.0.1    # load .env.local
ftfy==6.3.1             # repair WordPress latin1/utf8 + OEM (cp850) double-encoding mojibake
